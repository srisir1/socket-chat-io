/** @type {import('next').NextConfig} */
const nextConfig = {
        reactStrictMode: true,
        env:{
              
                HOST:"http://localhost:4000",
        }
};

export default nextConfig;
        
